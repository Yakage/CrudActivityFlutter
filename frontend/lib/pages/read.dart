import 'package:flutter/cupertino.dart';

class ReadStudent extends StatelessWidget {
  const ReadStudent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}